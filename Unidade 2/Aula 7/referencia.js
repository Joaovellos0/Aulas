let nome = "João";
const idade = 23;
const naotemcurso = false;
let carro
const listas = ["Texto", 100, true, {}, []];
const usuario = {
    nome: "João",
    email: "Não te interessa irmão"
};

// Isso é uma Função
function nomeDaFuncao(){

    // Isso é um loop
    for (let i = 0; i < 0; i++) {}
}

// Isso é uma condicional
if (1 == 1){

}